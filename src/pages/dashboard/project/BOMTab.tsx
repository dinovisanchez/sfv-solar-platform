import { EmptyState } from "@/components/ui/EmptyState";

export function BOMTab() {
  return (
    <EmptyState
      title="Lista de materiales (BOM)"
      description="Se generará automáticamente a partir del diseño: paneles, inversor, estructura, cableado y protecciones. Ver ROADMAP.md, Fase 3."
    />
  );
}
