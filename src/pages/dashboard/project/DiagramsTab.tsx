import { EmptyState } from "@/components/ui/EmptyState";

export function DiagramsTab() {
  return (
    <EmptyState
      title="Diagramas unifilares"
      description="Representación SVG dirigida por el modelo de diseño, coherente con el trámite ante el operador de red. Ver ROADMAP.md, Fase 3."
    />
  );
}
